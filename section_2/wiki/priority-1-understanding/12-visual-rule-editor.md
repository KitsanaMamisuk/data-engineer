# การออกแบบ Rule Editor แบบ Visual

## 1. Abstract (60 วินาที)

Visual Rule Editor คือส่วนติดต่อผู้ใช้ที่ให้สร้างและแก้ไข business rules โดยใช้การลาก-วาง (drag-and-drop) แทนการเขียน code ประกอบด้วยส่วนหลักๆ คือ: Rule Canvas (พื้นที่วาง rules), Component Palette (กล่องเครื่องมือ), Property Panel (ตั้งค่ารายละเอียด), และ Testing Panel (ทดสอบ) Visual Editor ทำให้ Business Analysts สามารถสร้างและแก้ไข rules ได้เองโดยไม่ต้องเขียน code

---

## 2. Core Principles (20/80)

หลัก 20% ที่ให้ผลลัพธ์ 80%:

1. **Visual Clarity** - เห็น logic ได้ชัดเจน
2. **Drag-and-Drop** - ลากวาง components
3. **Validation Feedback** - บอกทันทีเมื่อผิด
4. **Test Integration** - ทดสอบได้ใน editor
5. **Business-Friendly** - คนไม่ใช่ tech ใช้ได้

---

## 3. Mental Models

### Model 1: "กระดาน Trello"
- ลากการ์ดไปมาระหว่าง columns
- แต่ละการ์ด = 1 rule component
- มองเห็นภาพรวมได้ง่าย

### Model 2: "Scratch (ภาษาโปรแกรมสำหรับเด็ก)"
- ลาก blocks มาต่อกัน
- blocks ต่อได้เฉพาะที่ถูก type
- เห็น logic flow ชัดเจน

### Model 3: "Flowchart"
- กล่อง = actions
- ลูกศร = flow
- เพชร = conditions

---

## 4. Skill Tree

```
พื้นฐาน
├── ใช้งาน visual editor ได้
├── ลากวาง components ได้
└── ตั้งค่า properties ได้

กลาง
├── สร้าง complex rules
├── ใช้ decision tables
├── Test และ debug ใน editor
└── สร้าง templates

สูง
├── ออกแบบ custom components
├── Extend editor functionality
├── Optimize UX
└── Train users
```

---

## 5. What NOT to Learn

- **อย่าเรียน** UI framework implementation → เน้นการใช้งาน
- **อย่าเรียน** ทุก visual editors ในตลาด → เรียนแค่ที่ใช้
- **อย่าเรียน** UX design theory → เน้น practical usage
- **อย่าท่อง** ทุก component types → เรียนเมื่อต้องใช้
- **อย่าเรียน** accessibility standards ลึก → รู้พื้นฐานพอ

---

## 6. Real-world Usage

### การใช้งานใน BRMS:

1. **สร้าง rule ใหม่**
   - เลือก template หรือเริ่มจาก blank
   - ลาก conditions และ actions
   - กำหนด parameters

2. **แก้ไข rule เดิม**
   - เปิด rule ที่ต้องการแก้
   - แก้ไข components
   - Test และ save

3. **Review และ approve**
   - ดู visual representation
   - ตรวจสอบ logic
   - Approve หรือ request changes

---

## 7. Typical Pitfalls (10 ข้อ)

| # | ความผิดพลาด | วิธีเลี่ยง |
|---|-------------|-----------|
| 1 | Rule ซับซ้อนเกินในหน้าเดียว | แบ่งเป็น sub-rules |
| 2 | ลืม connect components | ใช้ validation |
| 3 | ไม่ test ก่อน save | บังคับ test |
| 4 | ไม่ใส่ descriptions | Require metadata |
| 5 | ใช้ defaults โดยไม่เข้าใจ | Review ทุก parameter |
| 6 | สร้าง spaghetti flow | Follow patterns |
| 7 | ไม่ใช้ templates | สร้างและใช้ templates |
| 8 | ไม่ name components ดี | Use meaningful names |
| 9 | ลืม else/default case | Validation checks |
| 10 | ไม่ version control | Use platform versioning |

---

## 8. Playbook (Step-by-step)

### สร้าง Rule ใน Visual Editor:

#### Step 1: เริ่มต้น
1. Login เข้า BRMS platform
2. ไปที่ Rule Editor
3. คลิก "Create New Rule" หรือเลือก template

#### Step 2: กำหนด Metadata
1. ตั้งชื่อ rule
2. เขียน description
3. เลือก category
4. กำหนด tags

#### Step 3: สร้าง Rule Logic
1. ลาก "Condition" block จาก palette
2. กำหนด field, operator, value
3. ลาก "Action" block
4. กำหนด output

#### Step 4: เพิ่ม Complexity (ถ้าจำเป็น)
1. ใช้ AND/OR connectors
2. เพิ่ม nested conditions
3. สร้าง multiple branches

#### Step 5: Test
1. คลิก "Test" button
2. ใส่ test data
3. ดู result
4. แก้ไขถ้าไม่ถูก

#### Step 6: Save และ Submit
1. คลิก "Save"
2. คลิก "Submit for Review"
3. รอ approval

---

## 9. Fast Practical Examples

### Example 1: Visual Editor Components

```
┌─────────────────────────────────────────────────────┐
│                 VISUAL RULE EDITOR                  │
├─────────┬───────────────────────────┬───────────────┤
│PALETTE  │      RULE CANVAS          │  PROPERTIES   │
├─────────┼───────────────────────────┼───────────────┤
│         │                           │               │
│[Cond]   │  ┌─────────────────┐      │ Selected:     │
│         │  │   IF Block      │      │ Condition     │
│[Action] │  │ ┌─────────────┐ │      │               │
│         │  │ │salary >= 30k│ │      │ Field: salary │
│[AND]    │  │ └─────────────┘ │      │ Op: >=        │
│         │  └────────┬────────┘      │ Value: 30000  │
│[OR]     │           │               │               │
│         │           ▼               │               │
│[NOT]    │  ┌─────────────────┐      │               │
│         │  │   THEN Block    │      │               │
│[Branch] │  │ ┌─────────────┐ │      │               │
│         │  │ │  APPROVED   │ │      │               │
│[Table]  │  │ └─────────────┘ │      │               │
│         │  └─────────────────┘      │               │
│[Func]   │                           │               │
│         │                           │               │
├─────────┴───────────────────────────┴───────────────┤
│  [Test] [Save] [Submit]                             │
└─────────────────────────────────────────────────────┘
```

### Example 2: Different Visual Representations

```python
# แบบที่ 1: Block-based (เหมือน Scratch)

"""
┌──────────────────────────────┐
│ WHEN application is received │
├──────────────────────────────┤
│ IF                           │
│   ┌────────────────────────┐ │
│   │ salary >= 30000        │ │
│   └────────────────────────┘ │
│   AND                        │
│   ┌────────────────────────┐ │
│   │ age between 20-60      │ │
│   └────────────────────────┘ │
├──────────────────────────────┤
│ THEN                         │
│   ┌────────────────────────┐ │
│   │ set status = APPROVED  │ │
│   └────────────────────────┘ │
└──────────────────────────────┘
"""

# แบบที่ 2: Flowchart-based

"""
    ┌─────────┐
    │  Start  │
    └────┬────┘
         │
    ┌────▼────┐
    │ Salary  │
    │ >= 30k? │
    └────┬────┘
    Yes  │  No
   ┌─────┴─────┐
   │           │
┌──▼──┐    ┌───▼───┐
│ Age │    │Reject │
│20-60│    └───────┘
└──┬──┘
   │Yes
┌──▼──────┐
│ Approve │
└─────────┘
"""

# แบบที่ 3: Decision Table

"""
┌──────────┬──────────┬──────────┬─────────┐
│  Salary  │   Age    │  Credit  │ Decision│
├──────────┼──────────┼──────────┼─────────┤
│   >= 50k │   ANY    │   >= 700 │ APPROVE │
│   >= 30k │  20-60   │   >= 600 │ REVIEW  │
│   < 30k  │   ANY    │    ANY   │ REJECT  │
└──────────┴──────────┴──────────┴─────────┘
"""
```

### Example 3: Condition Builder UI

```python
# UI สำหรับสร้าง Conditions

condition_builder = {
    "ui_type": "condition_builder",
    "components": [
        {
            "type": "field_selector",
            "label": "Field",
            "options": ["salary", "age", "credit_score", "debt_ratio"],
            "selected": "salary"
        },
        {
            "type": "operator_selector",
            "label": "Operator",
            "options": ["=", "!=", ">", ">=", "<", "<=", "in", "between"],
            "selected": ">="
        },
        {
            "type": "value_input",
            "label": "Value",
            "input_type": "number",  # auto-detected from field type
            "value": 30000
        },
        {
            "type": "logic_connector",
            "label": "Connect with",
            "options": ["AND", "OR", "END"],
            "selected": "AND"
        }
    ]
}

# Visual representation:
"""
┌─────────────────────────────────────────────┐
│  Condition Builder                          │
├─────────────────────────────────────────────┤
│                                             │
│  Field:    [salary        ▼]                │
│                                             │
│  Operator: [>=            ▼]                │
│                                             │
│  Value:    [30000        ]                  │
│                                             │
│  Connect:  (●) AND  ( ) OR  ( ) END         │
│                                             │
│  [+ Add Condition]                          │
│                                             │
└─────────────────────────────────────────────┘
"""
```

### Example 4: Action Builder UI

```python
# UI สำหรับสร้าง Actions

action_builder = {
    "ui_type": "action_builder",
    "action_types": [
        {
            "type": "set_value",
            "icon": "📝",
            "label": "Set Value",
            "fields": ["target_field", "value"]
        },
        {
            "type": "calculate",
            "icon": "🔢",
            "label": "Calculate",
            "fields": ["expression", "target_field"]
        },
        {
            "type": "call_function",
            "icon": "⚡",
            "label": "Call Function",
            "fields": ["function_name", "parameters"]
        },
        {
            "type": "send_notification",
            "icon": "📧",
            "label": "Send Notification",
            "fields": ["channel", "template", "recipients"]
        }
    ]
}

# Visual representation:
"""
┌─────────────────────────────────────────────┐
│  Action Builder                             │
├─────────────────────────────────────────────┤
│                                             │
│  Action Type: [Set Value       ▼]           │
│                                             │
│  ┌─────────────────────────────────────┐    │
│  │ 📝 Set Value                        │    │
│  │                                     │    │
│  │ Target Field: [decision      ▼]     │    │
│  │                                     │    │
│  │ Value:        [APPROVED      ]      │    │
│  └─────────────────────────────────────┘    │
│                                             │
│  [+ Add Action]                             │
│                                             │
└─────────────────────────────────────────────┘
"""
```

### Example 5: Testing Panel

```python
# Testing Panel UI

testing_panel = {
    "ui_type": "test_panel",
    "sections": [
        {
            "name": "Test Data Input",
            "type": "form",
            "fields": [
                {"name": "salary", "type": "number", "value": 35000},
                {"name": "age", "type": "number", "value": 30},
                {"name": "credit_score", "type": "number", "value": 720}
            ]
        },
        {
            "name": "Expected Result",
            "type": "assertion",
            "expected": {"decision": "APPROVED"}
        },
        {
            "name": "Execution Result",
            "type": "output",
            "result": None  # Populated after test run
        },
        {
            "name": "Execution Log",
            "type": "log",
            "entries": []  # Populated after test run
        }
    ]
}

# Visual representation:
"""
┌─────────────────────────────────────────────┐
│  Test Panel                                 │
├─────────────────────────────────────────────┤
│                                             │
│  📥 Test Data:                              │
│  ┌─────────────────────────────────────┐    │
│  │ salary:       [35000    ]           │    │
│  │ age:          [30       ]           │    │
│  │ credit_score: [720      ]           │    │
│  └─────────────────────────────────────┘    │
│                                             │
│  [▶ Run Test]                               │
│                                             │
│  📤 Result:                                 │
│  ┌─────────────────────────────────────┐    │
│  │ ✅ PASSED                           │    │
│  │                                     │    │
│  │ Output:                             │    │
│  │ {                                   │    │
│  │   "decision": "APPROVED",           │    │
│  │   "message": "ผ่านเกณฑ์ทั้งหมด"          │    │
│  │ }                                   │    │
│  └─────────────────────────────────────┘    │
│                                             │
│  📋 Execution Log:                          │
│  ┌─────────────────────────────────────┐    │
│  │ 1. Check salary >= 30000 → TRUE     │    │
│  │ 2. Check age >= 20 → TRUE           │    │
│  │ 3. Check age <= 60 → TRUE           │    │
│  │ 4. Execute: set decision=APPROVED   │    │
│  └─────────────────────────────────────┘    │
│                                             │
└─────────────────────────────────────────────┘
"""
```

### Example 6: Decision Table Editor

```python
# Decision Table Editor

decision_table_editor = {
    "ui_type": "decision_table",
    "columns": [
        {"name": "Credit Grade", "type": "input", "values": ["A", "B", "C"]},
        {"name": "Customer Type", "type": "input", "values": ["GOLD", "SILVER", "NORMAL"]},
        {"name": "Interest Rate", "type": "output", "data_type": "number"},
        {"name": "Max Loan", "type": "output", "data_type": "number"}
    ],
    "rows": [
        {"Credit Grade": "A", "Customer Type": "GOLD", "Interest Rate": 4.5, "Max Loan": 3000000},
        {"Credit Grade": "A", "Customer Type": "SILVER", "Interest Rate": 5.0, "Max Loan": 2000000},
        # ... more rows
    ]
}

# Visual representation:
"""
┌──────────────────────────────────────────────────────┐
│  Decision Table: Interest Rate Calculation           │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌─────────────┬──────────────┬────────────┬────────┐│
│  │Credit Grade │Customer Type │Interest Rate│Max Loan││
│  │  (input)    │   (input)    │  (output)   │(output)││
│  ├─────────────┼──────────────┼────────────┼────────┤│
│  │[A        ▼] │[GOLD      ▼] │[4.5      ] │[3M    ]││
│  ├─────────────┼──────────────┼────────────┼────────┤│
│  │[A        ▼] │[SILVER    ▼] │[5.0      ] │[2M    ]││
│  ├─────────────┼──────────────┼────────────┼────────┤│
│  │[A        ▼] │[NORMAL    ▼] │[5.5      ] │[1M    ]││
│  ├─────────────┼──────────────┼────────────┼────────┤│
│  │[B        ▼] │[GOLD      ▼] │[5.5      ] │[2M    ]││
│  └─────────────┴──────────────┴────────────┴────────┘│
│                                                      │
│  [+ Add Row] [- Delete Row] [Import CSV] [Export]    │
│                                                      │
└──────────────────────────────────────────────────────┘
"""
```

---

## 10. Diagnostic Questions

ตอบคำถามเหล่านี้เพื่อทดสอบความเข้าใจ:

1. Visual Rule Editor มีส่วนประกอบหลักอะไรบ้าง?
2. Drag-and-drop ช่วยอะไร?
3. ทำไม validation feedback ถึงสำคัญ?
4. Decision Table ต่างจาก IF-THEN blocks อย่างไร?
5. Testing Panel ใช้ทำอะไร?
6. ทำไมต้องมี Property Panel?
7. Visual Editor ช่วย BA อย่างไร?
8. เมื่อไหร่ควรใช้ Decision Table?
9. ทำไมต้องมี Execution Log?
10. Templates ช่วยอะไรใน visual editor?

---

## 11. Explainer for 5-year-old

ลองนึกถึง **เกมต่อ puzzle** นะ

- **ชิ้น puzzle** = blocks ต่างๆ (เงื่อนไข, การกระทำ)
- **กระดาน** = พื้นที่วาง rules
- **รูปภาพที่จะได้** = rule ที่สมบูรณ์

เราหยิบชิ้น puzzle มาต่อกัน ถ้าต่อถูกที่ รูปก็จะสวย
ถ้าต่อผิด ชิ้นจะไม่ล็อคกัน (validation)

Visual Editor ก็เหมือนกัน ลาก blocks มาต่อกัน ถ้าถูกก็ใช้งานได้!

---

## 12. Comparison Table

### ส่วนประกอบของ Visual Editor

| Component | หน้าที่ | ตัวอย่าง |
|-----------|--------|---------|
| **Canvas** | พื้นที่วาง rules | ตรงกลางหน้าจอ |
| **Palette** | กล่องเครื่องมือ | รายการ components ด้านซ้าย |
| **Property Panel** | ตั้งค่า component | ด้านขวา |
| **Test Panel** | ทดสอบ rule | ด้านล่างหรือ pop-up |
| **Toolbar** | Save, Submit, etc. | ด้านบน |

### Visual Styles

| Style | ข้อดี | ข้อเสีย | เหมาะกับ |
|-------|------|--------|---------|
| **Block-based** | ง่าย เห็นภาพ | ซับซ้อนไม่ได้มาก | Simple rules |
| **Flowchart** | เห็น flow ชัด | วาดยากขึ้น | Decision flows |
| **Decision Table** | กระชับ | ต้องมี pattern | Multiple conditions |
| **Form-based** | เรียบง่าย | ไม่เห็น flow | Configuration |

---

## 13. 7-Day Rapid Learning Roadmap

### สำหรับหัวข้อนี้โดยเฉพาะ (ใช้เวลา 1.5-2 ชั่วโมง)

| เวลา | กิจกรรม |
|------|---------|
| 0:00-0:15 | อ่าน Abstract และ Core Principles |
| 0:15-0:35 | ศึกษา Example 1-2 (Editor layout) |
| 0:35-0:55 | ศึกษา Example 3-4 (Condition/Action) |
| 0:55-1:15 | ศึกษา Example 5-6 (Test/Table) |
| 1:15-1:35 | ลอง follow Playbook step-by-step |
| 1:35-2:00 | อ่าน Pitfalls และตอบ Diagnostic Questions |

### เป้าหมายหลังจบ:
- [ ] รู้ส่วนประกอบหลักของ visual editor
- [ ] เข้าใจ flow การสร้าง rule
- [ ] รู้ว่าเมื่อไหร่ใช้ decision table
- [ ] เข้าใจความสำคัญของ testing
